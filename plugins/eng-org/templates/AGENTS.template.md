# AGENTS.md — universal AI entry point for {{PROJECT_NAME}}

> **Any AI agent or engineer picking up this repo reads this file FIRST.**
> It is intentionally tool-agnostic. Claude Code auto-loads `CLAUDE.md`;
> other tools (Cursor, Aider, Copilot Workspace, Codex, etc.) conventionally
> look for `AGENTS.md`. This file is the single shared starting point that
> points every one of them at the same context, in the same order.

This project is governed by the **eng-org** multi-agent engineering framework.
You do not need to have been here before to be productive — read the files
below in order and you will know WHAT exists, WHY it is built this way, and
WHEN each decision was made.

## Read these, in this order

1. **`CLAUDE.md`** — binding session rules (the framework block is owned by the
   `eng-org` plugin; project-specific rules live outside the FRAMEWORK markers).
2. **`eng-org.json`** — machine-readable manifest. Parse this first if you are a
   tool: it lists the stack, domains, governance doc paths, and the project
   registry. JSON, not prose, so it is fast and reliable to consume.
3. **`governance/ROLES.md`** — the role contracts (EM → TL → Dev → Test →
   Reviewer). Single source of truth for who does what.
4. **`governance/CONSTITUTION.md`** — the project rules. §H covers multi-agent
   rules specifically.
5. **`governance/MISTAKES.md`** — what has been broken before. Do not repeat it.
6. **`governance/ARCHITECTURE.md`** — the system shape and layering.
7. **`governance/graphs/`** — Mermaid diagrams of how everything *links*
   (module/layer dependencies, domain & data-model relationships, the
   requirement→task DAG, the role pipeline). Start here for a fast visual map of
   the project's relationships. Generated/refreshed by `/eng-org:graphyfy`.
8. Most recent files in **`governance/conversations/`** and
   **`governance/requirements/`** — the live "why/when" of in-flight work.

## The context model — where each answer lives

| Question | File(s) |
|----------|---------|
| **WHAT** was built? | `README.md`, `governance/requirements/REQ-*/spec.md` |
| **WHY** this way? | `governance/ARCHITECTURE.md`, per-REQ `DECISIONS.md` / ADRs |
| **WHEN** did decisions happen? | `DECISIONS.md`, `eng-org.json`, `governance/conversations/` |
| **HOW** is it built/run? | `README.md`, `governance/ARCHITECTURE.md` |
| **How does it all LINK?** | `governance/graphs/` (Mermaid — run `/eng-org:graphyfy` to refresh) |
| Current plan / state | `governance/requirements/` |
| How it's tested | `governance/COVERAGE_THRESHOLDS.md`, test suites |
| Everything, machine-readable | `eng-org.json` |

## Before you change anything

- Triage Mode A (trivial/docs) vs Mode B (full 5-role pipeline) per
  `CLAUDE.md` / `governance/ROLES.md`. When in doubt, Mode B.
- Run `node governance/scripts/check.mjs` and surface its output.
- Record every real decision with a date — never silently. The append-only
  decision log is what lets the *next* agent reconstruct intent unaided.

---
_Project: {{PROJECT_NAME}} · Backend: {{BACKEND_STACK}} · Frontend: {{FRONTEND_STACK}} · Domains: {{DOMAINS}} · Generated by eng-org on {{DATE}}._
